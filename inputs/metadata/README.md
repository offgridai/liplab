Optional per-case metadata files use the same stem as the WAV/transcript:

```text
inputs/metadata/case_000001.json
```

Example:

```json
{
  "speaker": "alfie",
  "sample_rate": 24000,
  "category": ["long_line", "punctuation", "enumeration"],
  "notes": ""
}
```
