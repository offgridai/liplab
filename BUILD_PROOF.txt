Build proof for liplab standalone repo

Commands run from repo root:
  rm -rf build
  cmake -S . -B build
  cmake --build build --config Release
  ./build/liplab_runner .

Result:
  CMake configure succeeded.
  CMake build succeeded.
  Harness run succeeded.
  Sample output: case001: planned=22 committed=22 handmade=15 mean_ms=75.4647

Revision note:
  TTS hint-stream usage has been removed from the standalone core/harness contract.
  The code now uses transcript + PCM WAV input only for planning and timing.
  No HintStream/TTSHint/text_progress/word_schedule API paths are present in src/lipsync or harness.
